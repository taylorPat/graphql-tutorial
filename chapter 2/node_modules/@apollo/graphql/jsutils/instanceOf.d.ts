/**
 * A replacement for instanceof which includes an error warning when multi-realm
 * constructors are detected.
 */
declare const _default: <T>(value: unknown, constructor: new () => T) => value is T;
export default _default;
