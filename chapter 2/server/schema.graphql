type Query {
    jobs: [Job!]
}

type Company {
    id: ID!
    name: String!
    description: String
}

type Job {
    id: ID!
    company: Company!
    title: String!
    description: String
    date: String!
}