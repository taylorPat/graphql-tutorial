type Query {
    job(id: ID!): Job
    company(id: ID!): Company
    jobs: [Job!]
}

type Company {
    id: ID!
    name: String!
    description: String
}

type Job {
    id: ID!
    company: Company!
    title: String!
    description: String
    date: String!
}